import React from "react";

// List is a container component
export function List({ children }) {
  return <div>{children}</div>;
}

// ListItem lists a book item
export function ListItem({ children }) {
  return <div>{children}</div>;
}
